# النشر على الخادم

**حميد حليوي** · الإنتاج: <https://hameed-hliwi.org> · الخادم: `/home/ubuntu/hameed-hliwi` تحت pm2

---

## النشر

على الخادم، أمر واحد:

```bash
cd /home/ubuntu/hameed-hliwi && bash scripts/deploy.sh
```

السكربت يجلب من `origin/main` ويقدّم بنفسه — **لا حاجة لـ `git pull` قبله**. وإن سحبتِ يدوياً على أي حال فلا ضرر: السكربت يقيس ما إذا كان **البناء القائم** مطابقاً للكود المسحوب، لا ما إذا كان المستودع مطابقاً للريموت.

| | |
|---|---|
| `--dry-run` | يعرض ما سيحدث ولا يغيّر شيئاً |
| `--force` | يعيد البناء حتى لو كان الكوميت منشوراً أصلاً |
| `--rollback` | يعود إلى ما قبل آخر نشر ناجح |

## ما يفعله السكربت

بالترتيب، وكلٌّ منها مشروط:

1. **يرفض العمل** إذا كان في الخادم ملف متتبَّع معدَّل. تعديل يدوي على الخادم إشارة خطر، لا شيء يُدهس بصمت.
2. يجلب `origin/main` ويعرض الكوميتات الجديدة، ثم يقدّم **تقديماً سريعاً فقط** (`--ff-only`) — لا كوميت دمج لم يطلبه أحد.
3. يقارن الكود المسحوب بآخر كوميت **بناه هو فعلاً** (محفوظ في `.git/deploy-current`). هذا هو السؤال الصحيح: «هل البناء المعروض هو الكود الحالي؟» — وليس «هل المستودع محدَّث؟». لولا ذلك لكان `git pull` يدوي قبل التشغيل كافياً ليخرج السكربت دون أن يبني شيئاً.
4. يثبّت الحزم **فقط** إذا تغيّر ملف قفل (`package-lock.json` للواجهة أو للخادم).
5. **يتوقّف تماماً** إذا تغيّر `backend/drizzle/` — لا يشغّل أي migration من تلقاء نفسه، بل يطلب منك أخذ نسخة احتياطية وتشغيلها يدوياً.
6. يحتفظ بنسخة من `dist/` الحالية، ثم يبني الواجهة. يبني الخادم **فقط** إذا تغيّر شيء في `backend/`.
7. يعيد تشغيل `hameed-hliwi` (والـ API فقط إن تغيّر الخادم). عملية pm2 غير موجودة تُتخطّى بتحذير لا بفشل.
8. يفحص `https://hameed-hliwi.org/api/v1/health` حتى عشر محاولات.

**إن فشل البناء أو فشل فحص الصحة، يتراجع تلقائياً**: يعيد `dist/` السابقة، ويعيد الكود إلى الكوميت الذي كان، ويعيد تشغيل العمليات. أي أن الخادم لا يبقى في حالة نصف منشورة.

## التراجع اليدوي

```bash
cd /home/ubuntu/hameed-hliwi
bash scripts/deploy.sh --rollback
```

يعود إلى الكوميت الذي كان قبل آخر نشر ناجح (محفوظ في `.git/deploy-previous`)، يعيد البناء والتشغيل والفحص.

## متغيّرات يمكن تجاوزها

| المتغيّر | الافتراضي |
|---|---|
| `APP_DIR` | `/home/ubuntu/hameed-hliwi` |
| `BRANCH` / `REMOTE` | `main` / `origin` |
| `WEB_PROCESS` / `API_PROCESS` | `hameed-hliwi` / `hameed-hliwi-api` |
| `HEALTH_URL` | `https://hameed-hliwi.org/api/v1/health` |
| `HEALTH_RETRIES` | `10` |

مثال: `WEB_PROCESS=web bash scripts/deploy.sh`

## ما لا يفعله السكربت — عمداً

- **لا migrations.** تغيير المخطط قرار، لا أثر جانبي للنشر. راجعي `BACKUP_AND_RECOVERY_RUNBOOK.md`.
- **لا نسخ احتياطي.** النسخ اليومي مجدول أصلاً على الخادم 03:15.
- **لا يلمس التطبيقات الأخرى** على نفس الخادم (`abooerp-backend`، `clotexerp-server`، `obada-server`).

## ما جرى التحقق منه

اختُبر على مستودعات محاكاة: رفض المجلد الخاطئ، رفض الشجرة المتسخة، التقديم السريع، تخطّي التثبيت حين لا تتغيّر التبعيات، **التراجع التلقائي عند فشل البناء** (أُعيدت `dist` والكود معاً)، مسار النجاح الكامل مع فحص صحة حقيقي، `--force`، و`--rollback`.

وأُعيد اختباره على محاكاة لحالة حقيقية وقعت في أول تشغيل على الخادم: `git pull` يدوي سبق السكربت، فرأى أن المستودع محدَّث وخرج **دون أن يبني** — وبقي الموقع يقدّم بناءً قديماً. هذا ما عالجه البند ٣ أعلاه، والحالة نفسها صارت جزءاً من الاختبارات.
