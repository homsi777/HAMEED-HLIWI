# Task: Generate a Complete, Evidence-Based Project Audit Report

You are working on a new independent project named **Hameed Hliwi Jewelry**.

This project is completely separate from any previous project.

Current known context:

* Frontend project based on React + Vite + TypeScript.
* The project is currently local and primarily frontend-focused.
* The project has been approved to become a cloud/browser-based system.
* Production domain reserved for the project:
  `https://hameed-hliwi.org/`
* Do not assume any backend, database, authentication, authorization, accounting, inventory architecture, or deployment architecture exists unless you actually find it in the repository.

Your task is **NOT to implement anything**.

Your task is to inspect the entire repository carefully and produce a **complete, detailed Markdown report describing the project exactly as it exists today**.

## Critical Rule

Do not guess.

Do not invent missing modules.

Do not infer business functionality merely from filenames unless the implementation confirms it.

Every important statement in the report must be based on actual evidence found in:

* source files,
* routes,
* components,
* pages,
* hooks,
* services,
* stores/state management,
* schemas/types,
* configuration,
* package files,
* API integrations,
* mock data,
* local storage usage,
* print templates,
* reports,
* navigation,
* permissions,
* authentication logic,
* server files,
* environment configuration,
* or any other repository content.

If something does not exist, explicitly state:

**Not found in the current repository.**

If something is only partially implemented, state clearly:

**Partially implemented.**

Do not describe planned behavior as existing behavior.

---

# 1. Project Executive Summary

Start the report with a clear overview of:

* What the application currently is.
* What business domain it appears to serve based on implemented functionality.
* Whether it is frontend-only, full-stack, or hybrid.
* Current maturity level.
* Main technologies used.
* How the application is currently started and built.
* Whether there is any real backend.
* Whether there is any real persistent database.
* Whether current data is mocked, hardcoded, local, browser-stored, or fetched remotely.
* Whether authentication currently exists.
* Whether permissions currently exist.
* Whether warehouse isolation currently exists.
* Whether the application is currently suitable for cloud deployment as-is.

---

# 2. Complete Repository Structure

Inspect the entire repository.

Document:

* top-level folders,
* application folders,
* important source folders,
* configuration files,
* public/static assets,
* scripts,
* print-related files,
* server-related files,
* environment files,
* routing files,
* utilities,
* reusable components,
* business modules.

Provide a readable tree or structured summary.

Do not dump thousands of irrelevant files such as dependencies.

Focus on project-owned code.

---

# 3. Technology Stack

Identify the actual technologies being used.

For each technology explain where and how it is used.

Include at least:

* React
* TypeScript
* Vite
* Tailwind CSS
* routing solution
* state management
* forms
* validation
* icons
* animations
* charts
* HTTP/network layer
* backend/server code if present
* Express if actually used
* Google GenAI if actually used
* localStorage/sessionStorage/indexedDB usage
* printing technologies
* PDF generation if present
* Excel/export technologies if present
* authentication libraries
* database drivers/ORMs if present

Also identify unused dependencies where reasonably verifiable.

---

# 4. Application Entry Points

Explain how the application starts.

Identify:

* main entry file,
* root component,
* routing initialization,
* layout/shell,
* providers,
* global state,
* theme initialization,
* RTL handling,
* localization,
* authentication initialization,
* data initialization.

Explain the real startup flow.

---

# 5. Navigation and Application Sections

Inspect the actual navigation/sidebar/menu/routes.

Produce a complete list of all currently available sections.

For every section include:

* Arabic name if present.
* English/internal name if present.
* route/path.
* corresponding page/component.
* purpose based strictly on implementation.
* whether fully implemented, partially implemented, placeholder, or static.
* main actions available.
* data displayed.
* filters.
* search.
* sorting.
* pagination.
* tables.
* cards.
* dialogs/modals.
* forms.
* context menus.
* export actions.
* print actions.
* reports.
* charts.
* quick actions.
* empty states.
* loading states.

This must cover every current section in the project.

---

# 6. Detailed Module-by-Module Audit

For every business module found in the repository, create its own report section.

Do not skip small modules.

For each module inspect all related code and document:

## Module Purpose

What the module actually does today.

## Screens

All pages/screens associated with it.

## Main Entities

Any business entities, interfaces, TypeScript types, schemas, objects, or models used.

## User Actions

For example:

* create,
* edit,
* delete,
* archive,
* restore,
* approve,
* cancel,
* print,
* export,
* search,
* filter,
* select,
* bulk operations,
* status changes.

Only include actions that really exist.

## Forms

Document all fields visible in forms.

Include:

* field name,
* type,
* required/optional,
* default value if present,
* validation if present.

## Tables / Lists

Document:

* displayed columns,
* actions,
* filtering,
* sorting,
* selection,
* pagination.

## Detail Views

Describe what information is displayed.

## Business Logic

Document any actual calculations or workflow rules found in code.

Examples may include:

* weights,
* gold purity,
* pricing,
* totals,
* discounts,
* taxes,
* invoice calculations,
* inventory quantities,
* profit calculations,
* customer balances.

Do not assume any jewelry-specific logic unless actually found.

## Statuses / Workflow States

List every state or status actually represented in code.

Explain allowed transitions if implemented.

## Printing

Explain every printing capability.

## Reporting

Explain every module-specific report.

## Data Source

For each module determine whether its data comes from:

* hardcoded arrays,
* mock files,
* generated values,
* localStorage,
* browser state,
* API,
* backend,
* database.

---

# 7. Dashboard Audit

Inspect the main dashboard carefully.

Document every:

* KPI card,
* value,
* chart,
* widget,
* recent activity,
* shortcut,
* notification,
* statistic.

For each KPI identify exactly where the value comes from.

Determine whether values are:

* hardcoded,
* calculated,
* mocked,
* dynamically loaded.

Explain every chart and its data source.

---

# 8. Sales / Invoice Functionality

If sales or invoicing exists, perform a very deep audit.

Document:

* sales list,
* invoice creation flow,
* invoice editing,
* invoice statuses,
* customer selection,
* products/items,
* quantities,
* weights,
* purity/karat,
* gold price,
* workmanship,
* discounts,
* taxes,
* payments,
* payment methods,
* totals,
* notes,
* salesperson,
* warehouse,
* invoice numbering,
* print templates,
* invoice preview,
* A4/A5/thermal formats if present,
* cancellation,
* returns,
* customer balance impact,
* stock impact.

Clearly distinguish implemented behavior from visual-only UI.

Inspect specifically the existing invoice print preview scripts/files such as:

* invoice-print-preview.html
* invoice-print-preview-a5-portrait.html

Describe their structure and current purpose.

---

# 9. Inventory / Warehouse Functionality

If inventory or warehouses exist, audit them deeply.

Determine whether the project currently has:

* multiple warehouses,
* warehouse selection,
* stock per warehouse,
* stock movements,
* transfers,
* adjustments,
* opening stock,
* stock count,
* reservation,
* damaged/lost items,
* item location,
* stock history.

Document what is actually implemented.

Most importantly, inspect whether records currently contain anything similar to:

* warehouseId,
* branchId,
* locationId,
* storeId.

Explain exactly how warehouse relationships currently work.

---

# 10. Jewelry-Specific Data Model

Inspect whether the system currently contains jewelry-specific concepts such as:

* gold,
* silver,
* gemstones,
* karat,
* purity,
* weight,
* net weight,
* gross weight,
* stone weight,
* workmanship,
* gold price,
* gram price,
* piece code,
* barcode,
* QR code,
* serial number,
* category,
* collection,
* supplier,
* manufacturer.

List only what actually exists.

For every found field explain where it is used.

---

# 11. Customers

If customer management exists, document:

* customer list,
* profile,
* fields,
* contact information,
* balances,
* transactions,
* sales history,
* payments,
* debts,
* credit,
* notes,
* filters,
* search,
* reports,
* printing,
* export.

Determine whether balances are real calculations or mock/static values.

---

# 12. Suppliers / Purchases

If present, document them to the same depth as Sales and Customers.

Include:

* purchase flow,
* supplier data,
* purchase invoice,
* receiving,
* payments,
* outstanding balance,
* inventory impact,
* returns,
* reports.

---

# 13. Accounting and Financial Features

Inspect the entire project for any accounting implementation.

Search for concepts such as:

* accounts,
* chart of accounts,
* journal,
* journal entry,
* debit,
* credit,
* ledger,
* cashbox,
* bank,
* receipt,
* payment voucher,
* customer balance,
* supplier balance,
* profit,
* loss,
* expenses,
* revenues.

Clearly state which exist.

Do not call something "accounting" merely because the UI shows financial totals.

Determine whether double-entry accounting exists or not.

---

# 14. Reports and Analytics

Create a complete inventory of all reports.

For every report specify:

* name,
* location,
* data source,
* filters,
* date ranges,
* KPIs,
* charts,
* table columns,
* export,
* printing.

Also distinguish real reporting logic from static UI prototypes.

---

# 15. Authentication

Inspect whether login/authentication exists.

Document:

* login screen,
* credentials,
* sessions,
* tokens,
* cookies,
* localStorage,
* logout,
* user profile,
* password management,
* password reset,
* backend verification.

Determine whether authentication is currently only visual/frontend.

---

# 16. Users, Roles and Permissions

This section is extremely important.

Inspect all code for:

* users,
* roles,
* permissions,
* claims,
* policies,
* access rules,
* menu visibility,
* route guards,
* action permissions,
* warehouse permissions.

Document exactly what exists today.

Determine whether:

* permissions are hardcoded,
* roles are hardcoded,
* permissions are only used to hide UI,
* there is any server-side enforcement,
* different users can access different modules,
* different users can access different warehouses.

Do not propose improvements yet.

This report must describe only the current state.

---

# 17. Warehouse/User Data Isolation

Perform a dedicated security audit of the current repository for this subject.

Determine whether the current application supports:

* one user assigned to one warehouse,
* one user assigned to multiple warehouses,
* warehouse manager role,
* warehouse sales employees,
* warehouse-specific records,
* warehouse-specific dashboards,
* warehouse-specific inventory,
* warehouse-specific sales,
* warehouse-specific customers,
* warehouse-specific reporting.

Also determine whether one warehouse could currently see another warehouse's data.

If no backend exists, explicitly explain why real isolation cannot currently be guaranteed.

Again: report current reality only.

---

# 18. Data Layer Audit

Identify every data source used in the project.

Search for:

* mock data,
* constants,
* arrays,
* fixtures,
* JSON files,
* localStorage,
* sessionStorage,
* IndexedDB,
* fetch,
* Axios,
* REST,
* GraphQL,
* Express endpoints,
* WebSockets,
* PostgreSQL,
* MySQL,
* SQLite,
* Supabase,
* Firebase,
* Prisma,
* Drizzle,
* Sequelize,
* Mongoose,
* other persistence mechanisms.

Create a clear data-flow diagram in Markdown where possible:

UI → State → Service → API → Database

But only include layers actually present.

---

# 19. Backend Audit

Inspect whether a backend exists.

If Express/server.js or any server code exists:

* inspect it completely,
* list endpoints,
* middleware,
* auth,
* persistence,
* validation,
* error handling,
* static hosting,
* deployment role.

Determine whether Express is actually part of the running application or merely an unused dependency/file.

---

# 20. API Audit

If any API calls exist, document:

* base URLs,
* endpoints,
* methods,
* request models,
* response models,
* authentication headers,
* error handling,
* retries,
* loading states,
* caching.

Do not expose secrets in the report.

Redact sensitive credentials/tokens if found.

---

# 21. State Management

Explain how state is currently managed.

Inspect for:

* React state,
* Context,
* reducers,
* Zustand,
* Redux,
* query libraries,
* custom stores,
* local persistence.

Identify which data is global and which is page-local.

---

# 22. UI/UX Architecture

Describe current UI structure:

* application shell,
* sidebar,
* header,
* dashboard layout,
* responsive behavior,
* RTL support,
* mobile behavior,
* desktop behavior,
* modals,
* drawers,
* forms,
* tables,
* notifications/toasts,
* loaders,
* skeletons,
* empty states,
* confirmation dialogs.

Identify reusable design-system components if present.

---

# 23. Styling and Design System

Document:

* Tailwind configuration,
* CSS architecture,
* colors,
* typography,
* spacing,
* shadows,
* buttons,
* cards,
* inputs,
* table styling,
* dark mode if present,
* themes,
* RTL.

Determine whether styles are centralized or duplicated.

---

# 24. Printing System

Perform a complete printing audit.

Search all files for:

* window.print,
* printable templates,
* invoice layouts,
* print CSS,
* page sizes,
* thermal printing,
* A4,
* A5,
* portrait/landscape,
* print preview.

List every print template and its dimensions/configuration.

---

# 25. Import / Export

Inspect for:

* CSV import,
* Excel import,
* Excel export,
* PDF export,
* JSON import/export,
* bulk import,
* download buttons.

Explain whether they work or are visual placeholders.

---

# 26. Search, Filtering and Sorting

Document global and module-specific search/filter mechanisms.

Identify whether filtering occurs:

* in memory,
* client-side,
* through API,
* through backend queries.

---

# 27. Notifications and Alerts

Inspect:

* toast notifications,
* alerts,
* system notifications,
* business notifications,
* reminders,
* approval alerts.

Describe their current implementation.

---

# 28. Audit Trail / Activity History

Determine whether the system records:

* who created a record,
* who edited it,
* timestamps,
* history,
* activity logs,
* deletion logs,
* approvals,
* audit events.

If absent, explicitly state it.

---

# 29. Security Audit — Current State Only

Without proposing architecture yet, identify the current security posture.

Inspect for:

* frontend-only authorization,
* exposed secrets,
* environment variables,
* API keys,
* hardcoded credentials,
* unsafe local storage,
* route protection,
* server validation,
* CORS,
* input validation.

Do not include actual secret values in the report.

---

# 30. Configuration and Environment

Document:

* environment variables,
* .env usage,
* Vite variables,
* ports,
* development configuration,
* build configuration,
* production configuration,
* domain references.

Determine whether `hameed-hliwi.org` is already referenced anywhere in the code.

---

# 31. Cloud Readiness — Current Facts

Evaluate only based on evidence whether the current application is ready to become a real cloud multi-user system.

Separate findings into:

* already available,
* partially available,
* missing.

Do not design the solution yet.

Focus on factual gaps such as:

* backend,
* database,
* authentication,
* authorization,
* persistent users,
* warehouse isolation,
* concurrency,
* deployment configuration,
* backups,
* server logging.

---

# 32. Code Quality and Architecture

Describe the current code organization.

Identify:

* modularity,
* component reuse,
* duplicated logic,
* business logic inside UI components,
* services,
* hooks,
* utilities,
* type organization,
* separation of concerns.

Highlight significant technical debt, but support each finding with code evidence.

---

# 33. Dead Code / Placeholder Features

Identify:

* placeholder pages,
* TODOs,
* mock buttons,
* non-functional actions,
* unused components,
* demo content,
* fake statistics,
* hardcoded sample records,
* unused dependencies.

---

# 34. Current Functional Coverage Matrix

Create a table with columns:

| Module / Feature | Exists | Functional | Data Source | Persistent | Backend Protected | Notes |

Include every major module found.

Use values such as:

* Yes
* No
* Partial

---

# 35. Current Permissions Matrix

If roles/permissions exist, build a matrix from actual code.

Example columns:

| Role | Module | View | Create | Edit | Delete | Approve | Print | Export | Warehouse Scope |

Do not invent missing permissions.

If no permission system exists, explicitly state that a matrix cannot be produced from the current implementation.

---

# 36. Current Warehouse Matrix

If warehouses exist, document:

| Warehouse-related feature | Current implementation | Evidence | Limitation |

Include:

* warehouse master data,
* stock isolation,
* sales isolation,
* users,
* managers,
* transfers,
* reports,
* permissions.

---

# 37. Important Findings

At the end of the report provide a concise but complete summary of the most important discoveries.

Separate into:

## Already Implemented

## Partially Implemented

## UI-Only / Mocked

## Completely Missing

## Highest-Risk Areas for Cloud Conversion

This section is descriptive only.

Do not start implementing fixes.

---

# 38. File References

For every major finding, include relevant file paths.

Example:

`src/pages/...`

`src/components/...`

`src/data/...`

`src/services/...`

This is important so another developer can verify every conclusion.

Where useful, include component/function/type names.

---

# 39. Required Output File

Save the final report as a Markdown file in the repository root:

`HAMEED_HLIWI_PROJECT_FULL_AUDIT.md`

The report must be detailed enough that a developer who has never seen the project can understand:

* what exists,
* how every section works,
* what data it uses,
* what is real,
* what is mocked,
* what is incomplete,
* and the current technical architecture.

Do not modify application behavior.

Do not refactor.

Do not add dependencies.

Do not implement backend/database/security changes.

This task is **inspection and reporting only**.

At the end, confirm the path of the generated Markdown report.
