# TASK 25 — تقرير: بطاقة لكل عيار، بلا رقم مكافئ، واختصارات من السجلات

الحالة: **منفَّذ ومُتحقَّق منه بتشغيل حقيقي**. **لم يُنشر** (§7).
تاريخ التنفيذ: 19 آب 2026 · الفرع `main`.

---

## 0. السؤال المعلّق — قبل أي تفصيل تنفيذي

**سؤال TASK 23 §3.4 ما زال بلا جواب.** لم تجب عليه، ولم أختر نيابةً عنك.

السؤال: هل تُغذّي **فواتير الشراء** دفتر ذهب الشركة مباشرة؟

- **(أ)** لا — رقم المخزون في لوحة القيادة يغطي «يزيد بالشراء وينقص بالبيع» أصلاً، ودفتر الذهب يبقى للعهدة والكسر والذمم. *(ترشيحي، من TASK 23)*
- **(ب)** نعم — ونربط البيع أيضاً ليخرج الوزن عند بيع القطعة.
- **(ج)** نربط فئتَي المعدن الخام فقط (`سبائك وليرات`، `ذهب كسر`).

**وملاحظتك المحفوظة لهذه المهمة** — أن الرصيد «يتأثر فقط بفواتير البيع والشراء حصراً» — **تصف الخيار (ب) أو (ج)، لا الوضع الحالي.** اليوم لا الشراء ولا بيع القطع يمسّان هذا الدفتر إطلاقاً؛ ما يحرّكه هو كسر المقايضة والعهدة والتعديل اليدوي.

**ما فعلتُه بناءً على ذلك:** بنيت العرض **غير ملتزم بأي جواب** — البطاقات تعرض ما في حساب الشركة فعلاً أياً كان مصدره. فإن اخترت (ب) أو (ج) لاحقاً، ستمتلئ البطاقات نفسها بلا تعديل عرض واحد.

---

## 1. بطاقة لكل عيار — بلا مكافئ عيار 24

الرقم الكبير المكافئ لعيار 24 **أُزيل تماماً** من هذه المجموعة. صار لكل عيار بطاقته بغراماته كما أُدخلت:

```
┌───────────────────────────────┐
│ عيار 21                       │   ← أولاً وأكبر، وبعرض البطاقتين على الشاشة الواسعة
│ 14500.000 غ                   │
└───────────────────────────────┘
┌──────────────┐ ┌──────────────┐
│ عيار 24      │ │ عيار 18      │
│ 1500.000 غ   │ │ 2500.000 غ   │
└──────────────┘ └──────────────┘
┌──────────────┐
│ عيار 14      │
│ 100.000 غ    │
└──────────────┘
```

- **عيار 21 يتصدّر دائماً** بثابت صريح `LEAD_KARAT = '21'` مع تعليق يشرح السبب (أكثر من 95% من ذهب المحل)، ثم البقية تنازلياً.
- على الهاتف: عمود واحد متراصّ. على الشاشة الواسعة: عمودان، وبطاقة 21 بعرضهما.
- كل بطاقة **قابلة للضغط** وتفتح سجل الأرصدة الافتتاحية (§3).
- سطر صغير أسفل البطاقات يذكر كم استُبعد من كسر المقايضة — لأن رقماً ينقص بلا تفسير أسوأ من رقم كبير.

**العمل خلفي؟ لا.** كما توقّعت الورقة، هذا **عرض فقط**. البيانات كانت صحيحة أصلاً: `holdings()` تُرجع `totalsExcludingScrap` بسطر لكل عيار منذ TASK 23.

**وأكّدت بالاختبار** أن `POST /gold/company-adjustment` يقبل:
- عدة عيارات في **نداء واحد** (`lines[]`) ✔
- وعياراً **لم يكن في القيد الأول** بقيد لاحق مستقل ✔ — كل نداء مستقل بمفتاح تكرار خاص، فلا حاجة لأي تعديل.

---

## 2. الرصيد لا يتأثر بزر «إضافة منتج جديد» — مُثبت لا مفترض

**الدليل: `backend/src/inventory/` بكامله لا يحتوي على إشارة واحدة** إلى `GoldPostingService` ولا `GoldDocumentsService` ولا جدول `goldLedgerEntries` ولا `goldTransactions`. البحث أُعيد **بعد** تغييرات TASK 23 لا قبلها.

الكاتبون في دفتر الذهب، حصراً وبالكامل:

| الملف | ماذا يكتب |
|---|---|
| `backend/src/sales/sales.service.ts` | كسر المقايضة الداخل مع فاتورة البيع |
| `backend/src/returns/returns.service.ts` | التزام الذهب عن مرتجع المبيعات |
| `backend/src/gold/gold-documents.service.ts` | ترحيل أثر المستندات |
| `backend/src/gold/gold.service.ts` | استلام/تسليم/تحويل/افتتاحي/**تعديل الشركة** |
| `backend/src/gold/weight-custody.service.ts` | عهدة الوزن لدى الأشخاص |
| `backend/src/gold/used-inventory.service.ts` | تحويل الكسر إلى مخزون مستعمل |

`inventory.service.ts` **ليس منها**. فإضافة منتج جديد أو تعديله أو حذفه **لا تُنتج غراماً واحداً** في هذا الدفتر — تماماً كما أردت: تسجيل قطعة للبيع ليس تصنيعاً لذهب جديد.

وتغييرا TASK 23 (`createCompanyAdjustment` و`holdings()`) لم يمسّا المخزون بأي شكل — كلاهما داخل `gold/` بالكامل.

---

## 3. اختصارات السجلات

**ما وجدتُه بالفحص أولاً:** `HistoryView` فيه تبويبان فقط — `invoices` و`weights` — وكلاهما مبنيّ على `historyApi` الذي يغطي **فواتير البيع وأسطر الأوزان المباعة فقط**. لا يعرض حركات دفتر الذهب إطلاقاً. فـ«اختصار إلى قيود الرصيد الافتتاحي» لم يكن ممكناً بمرشّح — لا توجد البيانات هناك أصلاً.

**ما بنيتُه:**

**شاشة جديدة `GoldOpeningsView`** (`activeTab === 'gold-openings'`) — بنفس نمط `WeightCustodyView` و`UsedGoldView` من TASK 22: هيدر بزر رجوع، وليست في القائمة الجانبية. تعرض حركات `/gold/transactions?type=opening` مع **مبدّل بين نوعين**:

- **ذهب المحل** — التعديلات اليدوية (بلا طرف)
- **أطراف تجارية** — الأرصدة الافتتاحية على عميل أو مورّد

الفصل بينهما مقصود: النوعان يحملان `opening` نفسه في الدفتر، وخلطهما على الشاشة هو بالضبط سوء الفهم الذي يجعل أحدهم يقيّد ذهب المحل على حساب زبون.

**المداخل إليها — ثلاثة، كلها إلى الشاشة نفسها:**

1. **في السجلات**، بطاقة بارزة أعلى الشاشة **تظهر في التبويبين معاً** (وُضعت فوق شريط البحث فلا تحتاج تمريراً).
2. **من بطاقات العيارات** في شاشة الأوزان — الضغط على أي بطاقة يفتحها.
3. مباشرة عبر التبويب `gold-openings`.

**الصلاحية:** الاختصار في السجلات يُمرَّر من `App.tsx` **فقط** لمن يملك وحدة `gold-weight-accounts`. فمن لا يملكها لا يرى زراً يقود إلى 403.

---

## 4. التحقق — تشغيل حقيقي

`backend/test/task25-gold-cards-per-karat.spec.ts` يشغّل التطبيق على منفذ 3027 ويضرب الـ API:

| الفحص | النتيجة |
|---|---|
| قيد واحد بثلاثة عيارات (21/24/18) | ✔ |
| **عيار رابع (14) بقيد لاحق مستقل** لم يكن في الأول | ✔ |
| كل عيار يحمل **غراماته المُدخلة بالضبط** — لا تحويل ولا دمج | ✔ |
| كل عيار سطر واحد، ولا تكرار لعيار | ✔ |
| **لا سطر في المجموعة يساوي الرقم المكافئ لعيار 24** | ✔ |
| القيدان ظاهران في سجل الأرصدة الافتتاحية | ✔ |
| القيدان بلا طرف (`partnerId = null`) — وهو ما يفصل النوعين | ✔ |
| قيود الأطراف ما زالت مُدرجة، فللشاشة جانبان يفصل بينهما | ✔ |
| **كل ما كتبه الاختبار عُكس**، والأرصدة عادت للغرام نفسه | ✔ |

**الفحوص الثابتة**
- الواجهة `tsc --noEmit`: **12 خطأ — نفس خط الأساس**، صفر خطأ جديد
- الخادم `tsc -p tsconfig.build.json --noEmit`: **صفر أخطاء**
- `npm run build`: نجح

### 4.1 ملاحظة على الأرقام التي ستراها

خط الأساس في قاعدة التطوير بعد استبعاد الكسر: **21K = −481.208** و **14K = −85**. أي أن البطاقات ستعرض أرقاماً سالبة.

هذا ليس عطلاً جديداً بل ما حذّرت منه في TASK 23 §2.1: استبعاد الكسر يكشف أن حساب ذهب المحل **لم يُفتتح برصيد قط**، فهو يسجّل الخروج بلا مصدر. **علاجه تسجيل رصيد افتتاحي** بزر «إضافة / تعديل وزن يدوي» — وهو الآن يعمل ومختبَر.

### 4.2 الفحص البصري على الهاتف — لم يُنفَّذ، وهذه المرة الرابعة

لا توجد في هذه البيئة أداة متصفح ولا Playwright ولا Puppeteer ولا التقاط شاشة. أقولها صراحةً بدل تخطّيها بصمت.

**القائمة الدنيا لما تفحصه بيدك** — أربعة أشياء لا أكثر:

1. **بطاقات العيارات** على 390px: عيار 21 أولاً وأكبر، والبقية تحته، وكل رقم كامل غير مقصوص.
2. **الضغط على بطاقة** يفتح سجل الأرصدة الافتتاحية، وزر الرجوع يعيدك.
3. **في السجلات**: بطاقة «سجل الأرصدة الافتتاحية» ظاهرة في **التبويبين** معاً.
4. **في شاشة السجل**: المبدّل بين «ذهب المحل» و«أطراف تجارية» يبدّل القائمة فعلاً.

---

## 5. خارج النطاق — لم يُمسّ

- **لم أقرّر §3.4** — سؤالك في §0.
- «وزن المصنعية» لم يُقترب منه إطلاقاً.
- `weight-custody.service.ts` و`used-inventory.service.ts` ودفتر الأطراف التجارية: بلا أي تغيير، ولم يُعَد أيٌّ منها إلى الواجهة.
- لا تغيير في مخطط قاعدة البيانات، ولا migration.

---

## 6. الملفات التي تغيّرت

**الواجهة**
- `src/components/GoldWeightAccountsView.tsx` — بطاقة لكل عيار، حذف الرقم المكافئ، تصدّر عيار 21، ربط البطاقات بالسجل
- `src/components/GoldOpeningsView.tsx` — **جديد**: شاشة سجل الأرصدة الافتتاحية
- `src/components/HistoryView.tsx` — بطاقة اختصار في التبويبين
- `src/App.tsx` — التبويب `gold-openings` وتمرير الاختصار حسب الصلاحية

**الخادم**
- لا شيء. المتطلب كان عرضاً كما توقّعت الورقة.

**الاختبار والتقرير**
- `backend/test/task25-gold-cards-per-karat.spec.ts` — **جديد**
- `TASK_25_GOLD_CARDS_PER_KARAT_REPORT.md`

**ملاحظة ترقيم:** ملف اختبار سابق كان يحمل `task25` (عمل حالة الفاتورة المرتجعة) أُعيدت تسميته إلى `task24b-returned-invoice-state.spec.ts` في الكوميت `3976e9a` تفادياً للتصادم مع هذه الورقة.

---

## 7. النشر — لم يحدث

**لم يُرفع ولم يُنشر.** وعندك الآن أربعة أعمال جاهزة غير منشورة على الخادم: TASK 23، TASK 24، TASK 24b، وهذه.

```bash
cd /home/ubuntu/hameed-hliwi/backend && npm run backup:scheduled   # إلزامية: TASK 24 يمسّ إلغاء الفواتير
git push origin main                                              # من جهازك
cd /home/ubuntu/hameed-hliwi && bash scripts/deploy.sh            # على الخادم
```
