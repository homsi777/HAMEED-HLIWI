# TASK 24 — تقرير: حذف صنف من المخزون كان يمنع إلغاء فاتورته أو إرجاعها

الحالة: **منفَّذ ومُتحقَّق منه بتشغيل حقيقي**. بندٌ واحد وجدتُه بالجذر نفسه **لم أُصلحه** وأبلغتُ عنه بدل ذلك، كما نصّت المهمة (§4).

تاريخ التنفيذ: 19 آب 2026 · الفرع `main` · **لم يُرفع إلى GitHub ولم يُنشر** (§7).

---

## 1. العطل

زر «حذف» في المخزون لا يحذف — **يؤرشف** (`archivedAt`). و`inventory.service.ts` كانت تسمح بالأرشفة دون النظر إلى حالة الصنف أو ارتباطه بفاتورة؛ تتحقق من رقم النسخة فقط.

ومسارا الاسترجاع كلاهما يشترطان صفاً **غير مؤرشف**:

| المسار | ما يحدث عند الأرشفة | الرسالة |
|---|---|---|
| إلغاء فاتورة بيع | استعادة المخزون تفشل | `Stock cannot be restored safely.` أو `Manual sale inventory entry cannot be reversed safely.` |
| قبول مرتجع مبيعات | `returns.service.ts:296` يرفض | `The inventory record for a returned line has been archived and cannot receive the return.` |

فصنفٌ بيع ثم حُذف يجعل فاتورته **غير قابلة للإلغاء وغير قابلة للإرجاع إلى الأبد**، بلا أي طريق للخروج. وهذا يفسّر لماذا فشلت كل محاولات نبيل ولم تنجح ولا واحدة: الحالة دائمة لا عابرة.

الفرضية أُثبتت قبل هذه المهمة على قاعدة التطوير بفاتورة حقيقية: الصنف غير مؤرشف يُلغى بـ 201، والصنف نفسه مؤرشفاً يرجع 409.

---

## 2. الإصلاح

### 2.1 المنع — `backend/src/inventory/inventory.service.ts`

`archive()` صارت ترفض أرشفة صنف تشير إليه فاتورة بيع **قائمة** (`status = 'posted'`)، برسالة عربية تُسمّي الفاتورة المانعة:

> لا يمكن حذف هذا الصنف: هو مباع ضمن الفاتورة **100905** وهي غير ملغاة. ألغِ الفاتورة أو سجّل مرتجعاً أولاً، ثم احذفه.

اختيار «فاتورة قائمة» تحديداً مقصود: فاتورة ملغاة لم تعد تحتجز شيئاً، فلا معنى لمنع الحذف بسببها.

### 2.2 فكّ الحصار عن الفواتير العالقة

عند **إلغاء بيع** أو **قبول مرتجع مبيعات**، إن كان الصنف مؤرشفاً تُرفع الأرشفة **داخل المعاملة نفسها** قبل أن يعمل منطق الاسترجاع القائم:

- `sales/sales.service.ts` — في `cancel()`، بعد فحص «آخر حركة» ومباشرةً قبل فرعَي البند اليدوي والبند العادي.
- `returns/returns.service.ts` — في `restoreSoldStock()`، مكان الرفض السابق بالضبط.

في الحالتين تُسجَّل **حركة مخزون** تُوثّق الإحياء (`unarchivedByReturn` / ملاحظة عربية على حركة الإلغاء)، فلا شيء يحدث بصمت.

المبرّر: الأرشفة في تلك اللحظة **قديمة بحكم التعريف** — إلغاء البيع أو قبول المرتجع يعني أن القطعة رجعت فعلياً، فلا يصحّ أن تكون «محذوفة» وراجعة في آن واحد. وهذا **ليس إذناً عاماً** بإحياء المؤرشف: لا يقع إلا كنتيجة مباشرة لقرار صريح على تلك الفاتورة بعينها.

ملاحظة على البند اليدوي: إلغاء البيع يؤرشف قيد البيع اليدوي أصلاً. فإن كان مؤرشفاً مسبقاً، يُرفع ثم يُعاد أرشفته — النتيجة النهائية صحيحة (مؤرشف)، والفارق أن الإلغاء ينجح بدل أن يفشل.

---

## 3. بند آخر بالجذر نفسه — أبلغتُ عنه ولم أُصلحه

`returns/returns.service.ts:337`، في مسار **مرتجع المشتريات**:

```ts
if (item.archivedAt || item.status !== 'in_stock')
  throw new ConflictException('The purchased stock is no longer in stock, so it cannot be returned to the supplier.');
```

الجذر نفسه: صنف اشتُري ثم حُذف لا يمكن ردّه إلى المورّد.

**لم أُصلحه** لأن §4 قالت: أبلغ ولا تُصلح خارج النطاق المشخَّص. وهناك سبب موضوعي أيضاً: مرتجع المشتريات يُخرج البضاعة من المحل، لا يُدخلها. فإحياء صنف لإخراجه فوراً قرار مختلف عن إحيائه لأنه عائد فعلاً. **إن أردته أُصلحه كمهمة صغيرة مستقلة.**

بقية أسباب الرفض في `returns.service.ts` فحصتُها ولا يشترك أي منها في هذا الجذر: تتعلق بحركة لاحقة على المخزون، أو بتغيّر متزامن، أو بحدود الكمية والوزن.

---

## 4. التحقق — تشغيل حقيقي على قاعدة التطوير

`backend/test/task24-archived-inventory.spec.ts` يشغّل تطبيق NestJS كاملاً على منفذ 3024 ويضرب الـ API فعلياً:

| الفحص | النتيجة |
|---|---|
| أرشفة صنف على فاتورة قائمة **مرفوضة (409)**، والرسالة **تُسمّي الفاتورة** وعربية | ✔ |
| الصنف يبقى غير مؤرشف بعد الرفض | ✔ |
| **بلا انحدار**: أرشفة صنف بلا فاتورة قائمة ما زالت تعمل (200) | ✔ |
| إلغاء بيع صنفه مؤرشف **ينجح**، والصنف يعود `in_stock` وغير مؤرشف | ✔ |
| قبول مرتجع صنفه مؤرشف **ينجح**، والصنف يعود `in_stock` وغير مؤرشف | ✔ |
| **الرسالة التي رآها نبيل** لم تعد تظهر في هذا السيناريو (مؤكَّدة نصّاً في الاختبار) | ✔ |
| بعد إلغاء الفاتورة، الصنف نفسه يُؤرشف عادياً | ✔ |

النتيجة: `TASK 24 — archived inventory no longer blocks cancellation or returns.`

**اختبارات الانحدار** على الأجزاء التي مسّها التعديل، وكلاهما **صفر فشل**:
- `test/task06-verification.ts` (المرتجعات الكاملة)
- `test/accounting-core.spec.ts` (يمرّ بالمبيعات والمرتجعات بكثافة)

**الفحوص الثابتة**
- الخادم `tsc -p tsconfig.build.json --noEmit`: **صفر أخطاء**
- الواجهة `tsc --noEmit`: **12 خطأ — نفس خط الأساس** المعتمد في TASK 22/23، صفر خطأ جديد (لم يُمسّ ملف واجهة واحد)

### 4.1 الفاتورتان في الإنتاج — لم أستطع فحصهما

`ad36aeee-09ad-4c8a-b231-d69f6378f812` و`2cc7e3f2-26e8-40de-ad0e-ae0d0ee626af`: **لا أستطيع الوصول إلى الإنتاج** (SSH مرفوض من مصنّف أذونات الجلسة، ولا اتصال لي بقاعدة الإنتاج). فلا أستطيع تأكيد انفكاك حصارهما — أستطيع فقط تأكيد أن الآلية التي حجزتهما زالت.

شغّل هذا على الخادم **قبل** النشر لتوثيق الحالة:

```sql
select si.invoice_number, si.status as invoice_status,
       ii.code, ii.name, ii.status as item_status, ii.archived_at
from sales_invoice_items sii
join sales_invoices si on si.id = sii.sales_invoice_id
join inventory_items ii on ii.id = sii.inventory_item_id
where si.id in ('ad36aeee-09ad-4c8a-b231-d69f6378f812',
                '2cc7e3f2-26e8-40de-ad0e-ae0d0ee626af');
```

كل صف فيه `archived_at` غير فارغ هو سبب الحجز. **وبعد النشر** جرّب الإلغاء أو المرتجع على الفاتورتين وقل لي النتيجة.

### 4.2 الفحص البصري على الهاتف — لم يُنفَّذ، وهذه المرة الثالثة

لا توجد في هذه البيئة أداة متصفح ولا Playwright ولا Puppeteer ولا التقاط شاشة. أقولها صراحةً بدل تخطّيها بصمت.

**عنصر واحد فقط يراه المستخدم** من هذه المهمة: رسالة الرفض العربية عند محاولة حذف صنف مبيع. تُعرض في شريط أحمر داخل شاشة المخزون (`InventoryView.tsx:457`، `text-xs`، بلا عرض ثابت، فتلتفّ طبيعياً) — سلسلة التمرير مؤكَّدة بالكود: `inventoryApi.archive` ← `mutate` في `useInventoryModule.ts:5` تضع `reason?.message` في `error` ← الشريط يعرضه.

**ما عليك فحصه بيدك على الهاتف:** افتح المخزون، جرّب حذف صنف مبيع، وتأكد أن الشريط الأحمر يظهر وأن الرسالة مقروءة كاملة على عرض 390px (هي جملة طويلة نسبياً وستلتفّ على ثلاثة أسطر تقريباً).

---

## 5. ما لم يتغيّر

- زر «حذف» في واجهة المخزون ما زال أرشفة لا حذفاً نهائياً — تغيّر **متى يُسمح بها** و**متى تُرفع تلقائياً** فقط.
- لا إحياء لأي صنف مؤرشف خارج معاملة الإلغاء أو المرتجع التي تسبّبه.
- عمل الأوزان في TASK 22/23 لم يُمسّ، وهذا الكوميت منفصل عنه تماماً.
- لا تغيير في مخطط قاعدة البيانات، فسكربت النشر لن يتوقف.

---

## 6. الملفات التي تغيّرت

**الخادم**
- `backend/src/inventory/inventory.service.ts` — منع أرشفة صنف على فاتورة قائمة
- `backend/src/sales/sales.service.ts` — رفع الأرشفة عند إلغاء البيع (+ استيراد `isNotNull`)
- `backend/src/returns/returns.service.ts` — رفع الأرشفة عند قبول مرتجع المبيعات
- `backend/test/task24-archived-inventory.spec.ts` — **جديد**

**التقرير**
- `TASK_24_ARCHIVED_INVENTORY_CANCEL_RETURN_FIX_REPORT.md`

**لم يُمسّ:** أي ملف واجهة، وأي شيء في `gold/`، وأي migration.

---

## 7. النشر — لم يحدث

**لم يُرفع إلى GitHub ولم يُنشر على السحابة.** التعديلات في كوميت محلي فقط.

بعد مراجعتك، وبالترتيب:

```bash
# ١) نسخة احتياطية على الخادم — إلزامية قبل هذا التعديل
cd /home/ubuntu/hameed-hliwi/backend && npm run backup:scheduled

# ٢) وثّق حالة الفاتورتين (الاستعلام في §4.1)

# ٣) من جهازك
git push origin main

# ٤) على الخادم
cd /home/ubuntu/hameed-hliwi && bash scripts/deploy.sh
```

ملاحظة: كوميت TASK 23 (`62bde95`) صار مرفوعاً على GitHub (رفعتَه بنفسك)، لكنه **لم يُنشر على الخادم بعد**. فتشغيل `deploy.sh` سينشر TASK 23 و TASK 24 معاً. إن أردت نشر هذا الإصلاح وحده أخبرني وأفصله على فرع مستقل.
