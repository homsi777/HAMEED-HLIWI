# TASK 26 — تقرير: السجلات تعرض حركات دفتر الأوزان فعلاً، لا زرّ اختصار فقط

الحالة: **منفَّذ ومُتحقَّق منه بتشغيل حقيقي**. **لم يُنشر** (§7).
تاريخ التنفيذ: 19 آب 2026 · الفرع `main`.

---

## 1. سوء الفهم — وقد كان سوء فهمي أنا

طلبتَ في TASK 25 §3 «بطاقة تعرض أوزاناً في السجلات». بنيتُ **زرّاً يخرج بك** إلى شاشة إدارة (`GoldOpeningsView`). الفرق جوهري: الزر ينقلك لتدير شيئاً، والسجل يدعك **تتصفّح** الحركات كما تتصفّح الفواتير في الشاشة نفسها.

هذه المهمة تضيف السجل الحقيقي. **والزر باقٍ كما هو** — لم يُحذف ولم يُعَد تصميمه.

---

## 2. §2.1 — ما الذي يقف خلف «الأوزان المباعة»، والقرار

### 2.1 ما وجدتُه بالكود

`backend/src/history/history.service.ts:271` (`soldWeightBase`) استعلام واحد على:

```sql
from sales_invoice_items i
join sales_invoices s on s.id = i.sales_invoice_id
```

مع طرح المرتجعات عبر `return_invoice_items`. أي أن التبويب **وزنُ بيعٍ على سطر فاتورة**: اسم الصنف، العيار، الوزن الصافي، السعر، العميل، البائع. **لا يمسّ `gold_transactions` ولا `gold_ledger_entries` إطلاقاً.**

### 2.2 القرار: **(ب) تبويب منفصل** — والسبب

اخترت الفصل لا الدمج، لسبب واقعي لا جمالي: **بيع قطعة ذهب لا يكتب شيئاً في دفتر الذهب**. تحقّقت من هذا سابقاً — الدفتر يتحرّك بكسر المقايضة والعهدة والتعديل اليدوي والتحويلات، لا ببيع المصاغ.

فلو مزجت «وزن مباع» مع «حركة دفتر» في قائمة واحدة، لقرأها الموظف على أنها دفتر واحد، ولجمع رقمين لا يُجمعان. وهذا بالضبط نوع الالتباس الذي أنفقنا TASK 22–25 في تفكيكه.

فصارت الشاشة ثلاثة تبويبات:

| التبويب | مصدره | ما يعرضه |
|---|---|---|
| سجل الفواتير | مستندات البيع | كما كان |
| **الأوزان المباعة** | `sales_invoice_items` | كما كان — **لم يُمسّ حرفاً** |
| **دفتر الأوزان** ← جديد | `gold_transactions` | حركات المعدن الفعلية |

(اختصرت عنوان التبويب الأوسط إلى «الأوزان المباعة» ليتّسع الثلاثة على عرض الهاتف — لا تغيير في محتواه.)

---

## 3. §2.2 — هل تكفي نقطة النهاية القائمة؟

`GET /gold/transactions` (لم تُبنَ في هذه المهمة ولا في 22–25) تُرجع لكل حركة: `date`, `type`, `typeLabel`, `status`, `sourceType`, `sourceNumber`, `transactionNumber`, `description`, `userNote`, `createdBy`, `partnerId`, `warehouseId`.

**تكفي لبطاقة سجل مقروءة** — وأثبت الاختبار أن **كل** حركة من 200 في قاعدة التطوير تحمل الحقول السبعة التي ترسمها البطاقة، بلا استثناء واحد.

### 3.1 ما ينقصها — مذكور لا مُضاف بصمت

ثلاثة أشياء غير موجودة **بشكل مُهيكل** في قائمة الحركات:

1. **العيار والوزن** — يعيشان في أسطر الحركة، ولا تُرجعها إلا `GET /gold/transactions/:id`. لكنهما **مكتوبان داخل `description`** («تسليم 11.000 غ عيار 21 إلى فلان»)، فالبطاقة تعرضهما نصّاً مقروءاً.
2. **اسم الطرف/الشخص** — يوجد `partnerId` بلا اسم. الاسم أيضاً داخل `description`.
3. **اسم الفرع** — يوجد `warehouseId` بلا اسم.

**الأثر العملي:** البطاقة مقروءة تماماً، لكن **لا يمكن الترشيح بالعيار** في هذا التبويب (لأنه غير مُهيكل). لو أردت ترشيحاً بالعيار، فهو يحتاج توسيع الاستجابة — أخبرني وأفعلها كمهمة صغيرة مستقلة، ولم أفعلها هنا لأن §2.2 نصّت على الإبلاغ لا الإضافة الصامتة.

### 3.2 انحراف واحد مقصود — أُعلنه بدل إخفائه

§2 اشترطت الترشيح **بمدى تاريخي كحدّ أدنى**، و`listTransactions` **لم تكن تدعمه** (كانت تدعم النوع والحالة والطرف والبحث فقط).

§3 قالت لا تمسّ **منطق العمل** في خدمات الذهب. فأضفت `dateFrom`/`dateTo` إلى استعلام القائمة وحده — شرط قراءة بحت، **بنفس السطرين الموجودين أصلاً** في `partnerStatement` في الملف نفسه. لا منطق عمل، ولا نقطة نهاية جديدة، ولا تغيير في أي حركة.

اعتبرتُه داخل روح §3 لأن بدونه لا يتحقق الحدّ الأدنى في §2. وإن رأيته تجاوزاً، حذفه سطران.

---

## 4. ما بُني

**تبويب «دفتر الأوزان»** داخل السجلات، ببطاقات بنفس أسلوب بطاقات الفواتير:

- **شارة نوع الحركة** بالعربية (رصيد افتتاحي، كسر مقايضة، استلام، تسليم، تحويل عيار…)
- **شارة المصدر** — وهذه تحلّ مشكلة قديمة: النوع وحده لا يميّز عهدة الشخص عن التسليم اليدوي (كلاهما `payment`). الآن يظهر «عهدة شخص» أو «يدوي» أو «فاتورة بيع» صراحةً.
- **البيان** الذي يحمل الوزن والعيار والطرف
- **ملاحظة المستخدم** إن وُجدت
- **رقم الحركة والمستند المصدر**، ومن سجّلها
- **الحركات المعكوسة تبقى معروضة** بشارة «معكوسة» وشطب — لا تُخفى

**المرشّحات:** المدى التاريخي وقائمة أنواع الحركات، مع الترقيم — كلها تستعمل حالة المرشّحات نفسها الموجودة في الشاشة.

**زر TASK 25 باقٍ كما هو** — أُكِّد بالكود، ما زال موجوداً في التبويبات.

---

## 5. التحقق — تشغيل حقيقي

`backend/test/task26-history-weight-records.spec.ts` (قراءة فقط، لا يكتب غراماً):

| الفحص | النتيجة |
|---|---|
| 200 حركة، **كلها** تحمل الحقول السبعة التي ترسمها البطاقة | ✔ |
| **حركة من كل نوع مطلوب** موجودة ومتصفَّحة: تعديل شركة `GOP-2026-033`، عهدة `GRC-2026-092`، يدوي `GRC-2026-062` | ✔ |
| كل تركيبات النوع/المصدر في البيانات مغطّاة بتسمية عربية (11 تركيبة) | ✔ |
| مرشّح النوع يضيّق ولا يسرّب: 33 حركة رصيد افتتاحي، لا غيرها | ✔ |
| **مرشّح التاريخ** الجديد: `dateFrom` و`dateTo` ونافذة يوم واحد — 20 حركة في 2026-08-18 وحده | ✔ |
| 12 حركة معكوسة ما زالت مُدرجة (لا تُخفى) | ✔ |
| **«الأوزان المباعة» يعمل كما كان** — نقطتا النهاية تستجيبان بلا تغيير | ✔ |

**الفحوص الثابتة**
- الواجهة `tsc --noEmit`: **12 خطأ — نفس خط الأساس**، صفر جديد
- الخادم `tsc -p tsconfig.build.json --noEmit`: **صفر أخطاء**
- `npm run build`: نجح

### 5.1 الفحص البصري على الهاتف — لم يُنفَّذ، وهذه الخامسة

لا أداة متصفح ولا Playwright ولا Puppeteer ولا التقاط شاشة في هذه البيئة. أقولها صراحةً بدل تخطّيها بصمت.

**خمس نقاط فقط تفحصها بيدك:**

1. **شريط التبويبات صار ثلاثة** — هل تتّسع على 390px بلا قصّ؟ (هذا أكثر ما يقلقني بصرياً، فالثلاثة في مساحة كانت لاثنين.)
2. **تبويب «دفتر الأوزان»**: البطاقات مقروءة، والبيان الذي يحمل الوزن والعيار غير مقصوص.
3. **قائمة أنواع الحركات** تحت التبويبات تبدّل النتائج فعلاً.
4. **مرشّح التاريخ** (زر التصفية) يعمل في هذا التبويب كما في غيره.
5. **تبويب «الأوزان المباعة»** كما كنت تعرفه تماماً — لا شيء تغيّر فيه.

---

## 6. الملفات التي تغيّرت

**الواجهة**
- `src/components/HistoryView.tsx` — تبويب «دفتر الأوزان»، بطاقات السجل، تسميات الأنواع والمصادر، مرشّح النوع

**الخادم**
- `backend/src/gold/gold.service.ts` — `dateFrom`/`dateTo` في `listTransactions` فقط (§3.2)

**الاختبار والتقرير**
- `backend/test/task26-history-weight-records.spec.ts` — **جديد**
- `TASK_26_HISTORY_WEIGHT_RECORDS_REPORT.md`

**لم يُمسّ:** `GoldOpeningsView`، `WeightCustodyPanel`، `UsedGoldPanel`، أي منطق عمل في خدمات الذهب، `history.service.ts`، وأي migration.

---

## 7. النشر — لم يحدث، ولم يُزعج ما هو معلّق

**لم يُرفع ولم يُنشر.** وهذا العمل **مستقل تماماً** عن الخمسة المعلّقة قبله (`62bde95`, `5c10818`, `143efad`, `3976e9a`, `7199f3d`) — لم يُعدَّل أي ملف منها عدا `HistoryView.tsx` الذي أضيف إليه تبويب دون مساس بما أضافته TASK 25.

وما زال معلّقاً عندك — كما اتفقنا، وليس من هذه المهمة:
1. استعلام حركة الـ 11 غرام وعكسها إن كانت تجربة
2. النسخة الاحتياطية على الخادم
3. الفحص البصري (11 نقطة من 23/24/24b/25، + 5 من هنا = **16 نقطة**)

وسؤال TASK 23 §3.4 (ربط الشراء) ما زال معلّقاً كما تركتَه.
